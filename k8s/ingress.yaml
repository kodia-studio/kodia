apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kodia-api
  namespace: default
  labels:
    app.kubernetes.io/name: kodia
    app.kubernetes.io/component: api
  annotations:
    # nginx ingress class
    kubernetes.io/ingress.class: nginx
    # TLS redirect
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # CORS
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://example.com"
    # Rate limiting (100 requests per second)
    nginx.ingress.kubernetes.io/limit-rps: "100"
    # Cert-manager certificate issuer (Let's Encrypt)
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: DENY";
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-XSS-Protection: 1; mode=block";
      more_set_headers "Referrer-Policy: strict-origin-when-cross-origin";

spec:
  ingressClassName: nginx
  tls:
  # TLS certificate (managed by cert-manager)
  - hosts:
    - api.example.com
    secretName: kodia-api-tls
  rules:
  # API endpoint
  - host: api.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kodia-api
            port:
              number: 8080

---

# Optional: Ingress for Prometheus metrics (internal only)
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: kodia-metrics
#   namespace: default
#   labels:
#     app.kubernetes.io/name: kodia
#     app.kubernetes.io/component: metrics
#   annotations:
#     kubernetes.io/ingress.class: nginx
#     # Basic auth for metrics endpoint
#     nginx.ingress.kubernetes.io/auth-type: basic
#     nginx.ingress.kubernetes.io/auth-secret: basic-auth
#     nginx.ingress.kubernetes.io/auth-realm: 'Metrics - Please login'
# spec:
#   rules:
#   - host: metrics.internal.example.com
#     http:
#       paths:
#       - path: /metrics
#         pathType: Prefix
#         backend:
#           service:
#             name: kodia-api
#             port:
#               number: 9090

---

# ClusterIssuer for Let's Encrypt (cert-manager)
# Install cert-manager first: helm install cert-manager jetstack/cert-manager
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@example.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - http01:
        ingress:
          class: nginx
